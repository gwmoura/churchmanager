<%= form_for(@category_event) do |f| %>
  <% if @category_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category_event.errors.count, "error") %> prohibited this category_event from being saved:</h2>

      <ul>
      <% @category_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
