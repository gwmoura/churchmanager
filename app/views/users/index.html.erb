<table class="table table-striped">
  <tr>
    <th>E-mail</th>
    <th>Igreja</th>
    <th></th>
    <th></th>
  </tr>
  <% @users.each do |user| %>
    <tr>
      <td><%= user.email %></td>
      <td><%= user.chruch_id == 0 ? user.chruch.name : '' %></td>
      <td></td>
      <td></td>
    </tr>
  <% end %>
</table>